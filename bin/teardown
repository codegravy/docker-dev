NAME=$1
if [ -z "$NAME" ]; then
	echo "No name entered. Cannot continue"
	exit 1
fi
docker start $NAME
docker exec $NAME /git/teardown
CODE=$?
if test $CODE = 0; then
	docker stop $NAME
	docker rm $NAME
	echo 'TEARDOWN COMPLETE'
else
	echo 'TEARDOWN FAILED'
fi
